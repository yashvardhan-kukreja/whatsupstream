coverage:
    max_report_age: off
    status:
      project:
        default:
          informational: true
          target: auto
          threshold: 100%
      patch:
        default:
          informational: true
          target: auto
          threshold: 100%

comment:
    layout: "reach,diff,flags,files,footer"
    behavior: default
    require_changes: no